Saturday, Feb 28 11:59 p.m.
Assignment 2
By: Ethan Bohling

Functions done:
countSubtreeUpTo3(); //Returns the size of the right child's subtree
createVine(); //Iteratively traverses left-skewed-vine and checks for any right child that have >= 2 size (using countSubtreeUpto3 function) and incorporates them into vine if size is >= 2
rotateLeft(); //Rotates the node's parent left, bringing up the provided node higher in the tre.


How to compile/run program
To compile: type make in terminal
To run: type ./aout
